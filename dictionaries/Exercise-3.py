# Exercise 3 — Add

# Add:

# learning_rate → 0.01

# Then print the dictionary.

model = {
    "name": "Random Forest",
    "accuracy": 0.92,
    "epochs": 50
}

model["learning rate"] = 0.01

print(model["learning rate"])